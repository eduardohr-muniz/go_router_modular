# 📊 Dashboard

Dashboard example syncing widgets via events.

## 🔄 Refresh Widgets
```dart
class DashboardModule extends EventModule {
  @override
  void listen() {
    on<RefreshDashboardEvent>((RefreshDashboardEvent event, BuildContext? context) {
      print('🔄 Refreshing widgets: ${event.sections.join(', ')}');
    });
  }
}
```

