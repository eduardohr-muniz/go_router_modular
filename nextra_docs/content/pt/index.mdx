---
title: go_router_modular
---

import { Callout, Cards } from 'nextra/components'

# go_router_modular

Injeção de dependência modular e roteamento por módulo construídos sobre o
[`go_router`](https://pub.dev/packages/go_router). Divida seu app Flutter em
**módulos** independentes — cada um dono das suas rotas, das suas dependências e
do seu ciclo de vida — e deixe o pacote conectar tudo e descartar
automaticamente.

## Por que módulos?

Um app Flutter que cresce tende a acumular uma única tabela de rotas gigante e um
container de dependências global. O `go_router_modular` quebra isso:

- **Encapsulamento** — um módulo declara as próprias rotas e binds. Nada vaza
  para fora a menos que você exporte.
- **Ciclo de vida automático** — as dependências de um módulo são registradas ao
  entrar nele e descartadas ao sair. Sem limpeza manual, sem vazamentos.
- **Roteamento familiar** — é `go_router` por baixo, então deep links, a barra de
  URL e o histórico do navegador funcionam como você espera.

## Um app mínimo

```dart filename="lib/main.dart"
import 'package:flutter/material.dart';
import 'package:go_router_modular/go_router_modular.dart';

Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await Modular.configure(
    appModule: AppModule(),
    initialRoute: '/',
  );
  runApp(const AppWidget());
}

class AppWidget extends StatelessWidget {
  const AppWidget({super.key});

  @override
  Widget build(BuildContext context) => ModularApp.router(title: 'My App');
}
```

```dart filename="lib/src/app_module.dart"
class AppModule extends Module {
  @override
  List<ModularRoute> get routes => [
        ModuleRoute('/', module: HomeModule()),
      ];
}
```

```dart filename="lib/src/modules/home/home_module.dart"
class HomeModule extends Module {
  @override
  FutureOr<void> binds(Injector i) {
    i.addSingleton<HomeController>((i) => HomeController());
  }

  @override
  List<ModularRoute> get routes => [
        ChildRoute('/', child: (context, state) => const HomePage()),
      ];
}
```

<Callout type="info">
  Primeira vez aqui? Comece pelo [Início Rápido](/pt/getting-started/quick-start)
  — ele coloca um app modular no ar em três passos.
</Callout>

## Explore a documentação

<Cards>
  <Cards.Card title="Primeiros Passos" href="/pt/getting-started/quick-start" arrow />
  <Cards.Card title="Rotas e Módulos" href="/pt/routes/routes-system" arrow />
  <Cards.Card title="Injeção de Dependência" href="/pt/dependency-injection" arrow />
  <Cards.Card title="Módulo de Eventos" href="/pt/event-module" arrow />
  <Cards.Card title="Testes" href="/pt/testing" arrow />
  <Cards.Card title="Micro Frontends" href="/pt/microfrontends" arrow />
</Cards>
